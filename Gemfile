source "https://rubygems.org"

# Release admin only (version bump, changelog, tag, release notes).
# Xcode Cloud builds, signs, and distributes; do not add build plugins here.
gem "fastlane", "~> 2.240"
